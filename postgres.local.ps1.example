# Copy this file to `postgres.local.ps1` and update credentials.
# `postgres.local.ps1` is git-ignored.

$env:BLOCKCHAIN_POSTGRES_CONNINFO = "host=127.0.0.1 port=5432 dbname=ip_chain user=postgres password=REPLACE_ME"
